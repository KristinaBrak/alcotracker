query Product($id: Int!) {
  product(id: $id) {
    id
    name
    link
    volume
    alcVolume
    priceMean
    priceMode
    discount
    category
    store
    prices {
      createdAt
      value
    }
  }
}
