query Product($id: Int!) {
  product(id: $id) {
    id
    name
    link
    volume
    alcVolume
    priceMean
    priceMode
    priceCurrent
    discount
    category
    image
    store
    prices {
      createdAt
      value
    }
  }
}
